# Wee Spurts — Game Bible

The single source of truth. When a decision is made, it's recorded here. Claude Code reads it automatically via `CLAUDE.md`. If code and Bible disagree, the Bible wins (or the Bible gets updated on purpose).

**Status:** Light / pre-prototype. Sections marked 🔒 are decided. Sections marked ❓ are deliberately open until the bowling prototype teaches us the answer (see `OpenQuestions.md`).

---

## 1. Vision 🔒

Wee Spurts is an online party game where the *fun is the friction*. It borrows Wii Sports' instantly-readable sports but swaps clean family competition for chaos: exaggerated physics, taunting, side-bets on each other's throws, and a comedic "drinking" gimmick. You play it in a Steam lobby with friends, laughing at each other, not at the game. Think Lethal Company, Meccha Chameleon, Thief Simulator, etc.

**One-line pitch:** Wii Sports if it you were able to throw a bowling ball at your friend as he's about to throw a strike, then you can go over to the bar, grab a beer and hit some slots in the corner. 

> **Naming note:** "Wee Spurts" is the game. "Friendslop" is the trending *genre* it belongs to (party games built around chaos + friends), not the title.

## 2. Core Pillars 🔒

Every feature must serve at least one. If it serves none, cut it.

1. **Chaos over precision.** Physics should surprise and amuse, not reward mastery. A perfect throw and a terrible one should both be funny. The games themselves should be fun and have replayability, not just getting bored in 30 seconds after you beat up your friend. 
2. **The table talk is the game.** Features exist to create reasons to yell, bet, gloat, and heckle. Downtime between turns is a feature, not dead air.
3. **Instantly readable.** A new player understands a minigame in 10 seconds. No manuals.
4. **Friends first.** Designed for a private lobby of people who know each other, not anonymous matchmaking.

## 3. Tone & content guardrails 🔒

- While gambling and drinking are present, we don't want to have to rate ourselves on Steam due to adult content and get screwed over on sales. Keep it funny but somewhat clean if possible?

## 4. Tech Stack 🔒

| Layer | Choice |
|---|---|
| Engine | Unity (latest LTS) |
| Language | C# |
| Netcode | Mirror + FizzyFacepunch (transport over Steam relay) |
| Steam API | Facepunch.Steamworks (lobbies, friends, invites, achievements) |
| Dev App ID | 480 (Spacewar) until we buy Steam Direct |
| Version control | GitHub + Git LFS |
| Art direction | Low-poly / stylized |
| Free assets | Kenney.nl, Unity Asset Store (free), Mixamo |
| AI 2D | Any image generator (UI, textures, concepts) |
| AI 3D | Meshy/Tripo/etc. for filler props only |
| AI audio | ElevenLabs + SFX generators |

Rationale for each choice lives in `Networking.md` (netcode) and the root roadmap. Don't re-litigate these mid-build; change them only by editing this table on purpose.

## 5. Systems overview 🔒

Build order is a dependency graph, not a calendar. See `Roadmap.md` for detail and `DefinitionOfDone.md` for exit criteria per system.

```
Core Framework  →  Steam Framework  →  Gameplay Framework  →  Bowling
                                                                 ↓
                                                       Menu / Lobby UI
                                                                 ↓
                                                       Slop Layer (bets, drink, taunts)
                                                                 ↓
                                                          Progression
                                                                 ↓
                                                        Second minigame
```

## 6. Folder & project conventions 🔒

See `CodingStandards.md`. Summary: the Unity project lives in `/Unity`; docs are law; every AI session is scoped to ONE system.

## 7. What makes Wee Spurts *different* ❓

This is the most important question and it CANNOT be answered on paper. The honest hypothesis: the differentiator is the "table talk" loop — betting on and heckling each other between turns — layered on chaotic physics. The prototype exists to prove or kill that hypothesis. Do not lock cosmetics, progression, or a marketing hook until a bowling game with the slop layer has actually made you and Braeden laugh. Tracked in `OpenQuestions.md`.

> **Candidate answer (July 2026, from `BLUEPRINT.md` research):** make the alley a *walkable social space* — spectators physically present, heckling from the gutter, bar + slots in the corner, comedic interference with the active thrower. It unifies all three pillars, fixes turn-based downtime, and no comp owns it. Test it in the first prototype; promote to a pillar if it's as fun as it sounds.

## 8. The throw — core bowling mechanic 🔒

The timing-based power meter is the heart of bowling and the concrete expression of Pillar 1. Timing well vs. poorly changes *what kind of throw* you get — not just how hard:

- **Good timing = control.** The ball goes exactly where aimed, with the spin you dialed. Clean, predictable, satisfying. This is the competitive hook that makes players keep trying to master it.
- **Bad timing = human fumble.** A mistimed release makes *the thrower* screw up — the ball hooks, chips, overcooks, or peels off. It always stays a believable bowling ball thrown badly by a person: never supernatural, never world-breaking. The miss must read as the player's incompetence, not the game malfunctioning.
- **Worse is funnier — non-linearly.** Small misses stay mostly fine; big whiffs go spectacular. Spectacle is reserved for genuine screwups. The extreme ends of the meter trigger a few *named* failure bits (Hook, Chip, Overcook, Whiff) with clear telegraphs, so the table instantly reads and heckles them (Pillar 2).
- **Skill ≠ domination.** Mastery buys consistency and style, not a runaway scoreboard. Inherent pin scatter plus the betting/heckling/drink layers keep any thrower from quietly running away with it, so *everyone's* throw stays watchable and funny.

Guiding line: a bad shot should feel like the best possible outcome of a screwup, not a punishment — **failure is content.** Full design detail + the open feel-knobs (good-zone width, gutter-vs-pin bias) live in `OpenQuestions.md`.

## 9. Powerups & special shots 🔒 (direction)

Wee Spurts leans **arcade, not sim** — closer to a "super battle golf" than to realistic bowling. The design runs **two rulebooks on purpose:**

- **Base throw = grounded (§8).** The default ball is a real bowling ball thrown by a person; comedy comes from human fumbles, never supernatural behavior (that would read as a bug).
- **Powerups = sanctioned cartoon logic.** Special shots and powerup balls MAY break physical realism — homing, explosions, wild trajectories — *because they're obviously special items*, clearly telegraphed, not the base ball misbehaving. This is where spectacle and the biggest bet-and-heckle moments live (Pillar 2).

Guardrails so powerups don't wreck balance:
- Powerups are **earned / limited**, never the default. Scarcity + risk offset their power, so §8's "skill = consistency, not domination" still holds.
- Spectacle stays **contained**: it affects the pins / the shot, never permanently damages the lane/map and never spirals — "no map collapsing" still applies.

First candidate: the **Nuke Shot** (green-zone-only; detonates on the pins if nailed, in-hand if missed). Speced in `BowlingFeelIdeas.md`; greybox prototype pending.

---

## Change log
_Chronological, newest at the bottom. The 🔒/❓ sections above are the fast-read layer — what's true right now. This log runs long and detailed on purpose (most lines are tagged "not a design decision": bugfixes and implementation notes kept for archaeology, and because Tony + Braeden are learning Unity/C# through this project) rather than staying a terse decision list. Skim by date and **bold** text if you just want the highlights._
- _(date)_ — Bible created, light/pre-prototype version.
- 2026-07-21 — Repo restructured for Claude Code (`CLAUDE.md`, `.claude/` agents & commands). Chat-paste personas retired to `Docs/archive/`. `BLUEPRINT.md` added (market research, phases, gates). `Marketing.md` added. Walkable-alley hypothesis logged in §7 and `OpenQuestions.md`.
- 2026-07-21 (later) — `PLAYBOOK.md` added: full AI/human task ledger. Phase 0+1 code written and staged in `_Staging/` (core framework, unit-tested scorer, ball/pins/input/camera, one-click greybox scene builder). Transport corrected: FizzySteamworks → **FizzyFacepunch** (must match Facepunch.Steamworks).
- 2026-07-21 (later still) — Tony decided: characters = **bean-people ragdolls**; world = **bright Wii-clean**. `Docs/ContentPlan.md` added: full asset inventory (all free/CC0 sources named), UI kit + fonts, audio plan, art-pass schedule.
- 2026-07-21 (revision) — Characters revised on Tony's call: **Phasmo-style low-poly humanoids** (free rigged placeholders + Mixamo; own AI models swap in later); beans parked as fallback. `Docs/AssetWorkbench.md` added: Tony's hands-on AI asset guide (prompt templates, pipelines, priority queue).
- 2026-07-22 — Bugfix, not a design decision: `GreyboxSceneBuilder.cs` ball bounce material now sets `bounceCombine = Maximum` so a ball's own `BallConfig.Bounciness` always wins over the lane floor's, instead of averaging with it. Fixes bouncy-ball feel testing (ball was "slamming down" even at high Bounciness) and means future ball-feel power-up variants (bouncyball, cannonball, etc.) never need a matching floor material.
- 2026-07-22 (later) — Bugfix, not a design decision: `BallConfig.Bounciness` wasn't reaching the ball at all when swapped via the sandbox switcher — `GreyboxSceneBuilder` baked bounciness onto the ball's physic material once, at scene-build time, from whichever `BallConfig` was default then. `BowlingBall.Launch()` now restamps the collider's physic-material bounciness from the active config on every throw. Also seeded the switcher with `BouncyBall`/`Cannonball` automatically on scene build, so they no longer need manual Inspector wiring.
- 2026-07-23 (design direction) — Vision nudged toward **arcade "super battle golf"** feel (Tony's call): grounded skill-based base throw (§8) + an over-the-top **powerup layer** as a *sanctioned exception* to §8's realism rule. New §9 records the two-rulebook split and its guardrails (powerups earned/limited; spectacle contained, no map damage). First powerup candidate — the **Nuke Shot** — parked/speced in `BowlingFeelIdeas.md` as a greybox prototype candidate.
- 2026-07-22 (design decision) — **Throw mechanic locked** (new §8). Miss-timing = *grounded human-fumble chaos*, not floppiness or world-breaking: bad timing distorts direction/spin (funny axes), good timing stays clean/predictable (the competitive hook). Escalation is **non-linear** — small misses mild, big whiffs spectacular — with named failure archetypes (Hook/Chip/Overcook/Whiff) hand-placed at the meter extremes. Skill = consistency/style, not domination (pin scatter + slop layers keep it level). Feel-knobs (good-zone width, gutter-vs-pin bias) stay open in `OpenQuestions.md`. Refined from an earlier Sonnet draft of the same idea.
- 2026-07-22 (later still) — Sandbox dev tool, not a design decision: added a quick-reset-frame debug key (`F`) so Tony can retry one throw setup repeatedly instead of playing through pin counting every time. `BowlingScorer.ResetCurrentFrame()` discards only the current frame's rolls (earlier frames + `IsGameOver` untouched); QA-reviewed (qa-engineer agent) for scoring-integrity edge cases across strikes/spares/10th-frame bonus rolls — no bugs found, all traced cases correct. QA flagged one pre-networking item: sandbox keys (`F`, `R`, `1`-`9`) mutate `BowlingGameController`/`BowlingScorer` with no host-authority check, which is fine now (not networked) but must be gated before Mirror lands on this class.
- 2026-07-22 (later still) — Sandbox dev tool, not a design decision: added an AIM-phase visual preview (new `AimPreview.cs`) — the ball now visibly slides with lateral aim input and a curved line previews direction + spin, purely cosmetic, verified to never touch `LaunchParameters` or the resolved throw. Confirms §8's mechanic isn't implemented in code yet: `BallLauncher` currently gives direct, undistorted player control over angle/spin with no timing-driven distortion and no consumption of `LaunchParameters.Seed` — that's tracked as future work, not a doc/code contradiction. QA-reviewed (qa-engineer agent): caught and fixed a real bug pre-merge (`AimPreview`'s editor-wired references weren't `[SerializeField]`, so they'd go null on scene reload) plus a `DebugHud` power-bar-stuck-on-screen regression introduced in the same session.
- 2026-07-25 (presentation decision + sandbox prototype) — **Throw camera is now a scripted six-beat cinematic move** (`ThrowCameraSequence`), replacing the old two-mode aim/chase camera as the default. Wii Sports read: "you're up" front-on beat → swing behind into the over-the-shoulder aim framing → a slow push-in on the thrower while the power meter charges → whip out low over the lane at release → travel with the ball → arrive at the pins as it hits. Two calls Tony made by playing, both recorded here because they overrode the original spec: (1) the front-on "you're up" beat is a **turn-start hold only** and must NOT be the aim framing — you cannot aim while looking at your own face, and `AimPreview`'s guide line would point at the camera; the aim phase keeps the existing over-the-shoulder view as its basis. (2) The charge beat **pushes IN slowly toward the character** rather than pulling back to a wide shot, and the old separate "swing through" beat was deleted into it — one slow creep that then waits reads better than two beats. Camera travel down the lane is capped at ~3/4 (`TravelCapLane01`) so the ball runs the last stretch alone, as the Wii does; documented trade is that while the cap binds it overrides the lanes-in-frame framing solve. Presentation only — never touches `LaunchParameters`, physics or scoring — and the Nuke Shot keeps its own camera outright. Does NOT foreclose `OpenQuestions.md`'s walkable-alley question (Tony: a walkable space would cut to this cinematic for the throw itself, per the drag-to-the-lane idea at `OpenQuestions.md:22`).
- 2026-07-25 (bugfix, not a design decision) — Character setup fixed in the GENERATOR (`CharacterSetupTool`), not in hand-edited asset state, so a reimport can't reintroduce either bug. (1) The thrower clapped forever because **a Mixamo FBX is not one animation** — each carries twelve takes (the Quaternius set first, the downloaded motion last as `mixamo.com`), and the tool renamed all twelve to one name then picked "the first clip in the file". Every state in the controller was bound to `Man_Clapping`; no Mixamo clip had ever played. Now only the `mixamo.com` take is imported, and states match clips by name. (2) The model rendered stretched because its Scale Factor (0.4) no longer matched its clips' (1) — under a Generic rig clips drive absolute bone positions in the clip's units, so the skeleton was pulled apart. Scale Factor is now pinned identical across all fourteen FBXs, and display size is a single uniform scale on the prefab root (`CharacterDisplayScale`, default 0.4). **Rig type re-examined and deliberately left Generic:** these Quaternius bodies export a Blender IK control rig (`Foot.L`/`Foot.R` are root-parented IK targets with `PoleTarget` siblings, leg chain dead-ends at `LowerLeg_end`), so Humanoid cannot validate — confirming `OpenQuestions.md`'s existing entry and Tony's option (a). Mixamo retargeted server-side at export, so Generic plays the clips correctly. Presentation/setup only — `LaunchParameters`, physics and scoring untouched, and no camera value changed. Deviation noted: the 0.4 knob is a documented `const` on the editor tool rather than a ScriptableObject, because it is consumed at prefab-generation time and could not be a runtime tweak either way (Tony's call).
- 2026-07-25 (scoreboard decision) — **The player-facing score is now the LIVE score, not the resolved score.** `BowlingScorer.GetProvisionalTotal()` added (purely additive; `AddRoll`/`GetTotal`/`GetFrameTotals` untouched) and `DebugHud`'s `TOTAL` switched to it. Standard ten-pin bookkeeping cannot total a strike's frame until its two bonus rolls exist, so the headline number sat at `0` for two turns after a strike — correct on paper, unreadable in a party game, and glaring with a guaranteed-clear Nuke. The per-frame boxes still show formal resolved totals (blank until resolved), so the scorecard itself stays honest; only the headline number went live. Verified against the official total on the standard reference games and across 200,000 randomised games (never decreased, never exceeded, always converged exactly). Not a rules change — a display decision.
- 2026-07-25 (design decision, §8) — **Spin became a 2D selector, and spin never costs power.** `LaunchParameters.Spin` went from one float to a `Vector2` clamped inside the unit circle: **X = side spin (the hook), Y = roll vs skid** — topspin grips and curves EARLY (straighter overall, slight forward drive), backspin SKIDS then breaks LATE and harder. Two calls recorded here because they're deliberate deviations. (1) **Off-centre placement does NOT reduce power**, unlike pool: power stays entirely on the timing meter, because two systems fighting over speed muddies the one thing §8 says the player is meant to master. (2) **The mistiming Hook survives intact and is ADDITIVE with player spin** — separate forces, separate config knobs (`SpinCurveForce` vs `HookForceMagnitude`), neither scaling the other, so a hard release still slices right even when the player dialled left. That tension is the point: the fumble fights the intent rather than replacing it, which keeps §8's "a miss reads as the player's incompetence" true even for a player who has mastered spin. Schema change made now on purpose — networking doesn't exist yet, so this is the cheapest moment it will ever be. Feel-knobs (`RollSkidHookScale`, `SpinRampDistance`, and the spin-vs-Hook balance) are Inspector-tunable and stay open. QA-reviewed (qa-engineer agent): both spin-math invariants independently re-derived, determinism and the seeded RNG stream confirmed intact, no hallucinated APIs; five defects found and fixed pre-merge (a URP transparency knob that silently did nothing, a stuck mouse-drag that could apply spin the player never dialled, and three generator bugs in the character material path). **One open item deliberately NOT decided here** — the release fumble's `ConeSpinJitterMagnitude` kick is folded into the player's spin vector, so dialling topspin damps most of it and backspin roughly triples it; whether a player should be able to partly dial away their own fumble is Tony's call, logged in `OpenQuestions.md`.
- 2026-07-26 (set dressing, not a design decision) — **The venue around the lanes now exists as a one-click greybox** (`AlleyGreyboxBuilder` + `AlleyLayoutConfig`), built to test `OpenQuestions.md`'s walkable-alley hypothesis with real geometry instead of on paper: 12 lanes, per-lane approach clusters, an L-shaped front desk, a bar with stools, a raised casino nook with a chamfered corner and a 5-step stair, a recessed card-dealer alcove, open-floor seating, a cosmetics counter and an entrance. Laid out entirely in the **playable lane's coordinate frame** — origin at the foul line on the lane's centreline, `+Z` down-lane, `x = 0` the real lane rather than the middle of the room — so the venue and the lane cannot drift apart. Three calls Tony made from the blueprint: (1) approach order is **ball return → kiosk → monitor** (the drawing won over an earlier note; it's also how you actually use one — grab ball, then console); (2) the playable lane's approach cluster is **ghosted, not deleted** — collider-free and pushed behind `z = -3.7` so it clears the throw camera, because a bare slot leaves a hole exactly where the eye goes when judging the venue; (3) lane pitch dropped to **~2m with slim backdrop gutters** (the real lane keeps its own wider rails) — at `LaneConfig.NeighbourLaneSpacing`'s 2.9 the room ran 41×43m, and a 40m walk between turns is a commute in a game whose downtime is meant to be the fun part. Nobody reads the narrower backdrop gutters at that distance. **Two safety rules are enforced in code, not by discipline:** every collider created is audited against a playable-lane exclusion volume and errors loudly if it lands inside, and all backdrop/decorative geometry is built collider-free — so nothing here can ever deflect the ball or block a pin. Superseded `GreyboxSceneBuilder`'s four cosmetic neighbour lanes, which overlap the new bank; retire them with the **existing** `LaneConfig.BuildNeighbourLanes` tickbox (`GreyboxSceneBuilder.cs` itself was not modified). Named anchor markers dropped for the entrance, casino, card dealer, bar, front desk, cosmetics counter and each lane's kiosk, so the walk-up-to-a-lane game start and the drag-to-the-line turn transition (`OpenQuestions.md:22`) get real transforms instead of hard-coded numbers. Greybox only — no gameplay, no networking, no scoring touched.
- 2026-07-26 (later — set dressing + a design call on §7) — **AlleyLayout v2: the venue became a banded, walkable, looping social floor.** v1 was a room with props round the edges; v2 is a stack of Z bands derived from depth fields — west band 6.5m (service spine + casino + card dealer), concourse 4.0m, six sunken settee pits 3.0m, paired ball returns 1.8m, and a **completely clear 4.5m approach** because that is where the throw camera lives. The bands sum to 19.8m, so the room came out 0.7m *shallower* than v1 (41.3m vs 42.0m in Z, X unchanged) — the restructure does not lengthen the walk, which was Tony's objection to the v1 scale. `GreyboxSceneBuilder.cs` untouched again. **One decision belongs in §7, not just the changelog: the turn-start reveal stays.** Beat A looks back at the thrower from 1.8m, so the settee pit behind him — and whoever is sitting in it — is in frame for the "you're up" hold. That was surfaced as a feel call and Tony kept it, on the grounds that it sells the walkable-alley hypothesis in one frame for free. It is therefore load-bearing: the pit behind the playable lane is deliberately solid, walkable and *not* ghostable, and the build logs what is in that corridor every time as information rather than a defect, so no future venue change can silently alter the opening shot. Three more calls, logged in `OpenQuestions.md`: all six pits get the full treatment; players may walk down backdrop lanes (a feature, not a bug); and circulation is **three** categories — primary route 4.0m, secondary 2.5m, threshold 1.2m — with thresholds (pit entrances, counter walk-ins, doorways, alcove mouths) exempt from the secondary rule, which is what makes the 1.6m gaps between pits legal and settles that `LanePitch` does not have to grow. **Two safety systems, both in code rather than by discipline.** The old single exclusion volume split into three checks, because it had been protecting the ball *and* the camera corridor at once and that conflation is exactly what would have made a legitimate settee pit illegal: a hard ball-volume check, a hard approach-band check that is strictly stronger than v1's (it fails on any created object — collider, **renderer or anchor**), and an informational camera-corridor listing. And a new `AlleyCirculationAudit` that *proves* the floor is walkable instead of asserting it: a 2.5D flood fill over a 0.25m grid that reports anchor reachability, per-category clear widths, unclassified pinches, and the exact number of independent loops (the first Betti number of the walkable region — 12 at the defaults, so the floor genuinely loops). Categories and directions of travel are **declared by the builder, never inferred from geometry**; anything it measures that nobody declared is reported as UNCLASSIFIED for Tony rather than silently assigned. The audit **reports and never fixes** — the pit interiors currently measure too tight and that has been left alone on purpose. Greybox only: no gameplay, no networking, no scoring, no physics touched.
- 2026-07-26 (set dressing + a new verification tool, not a gameplay decision) — **AlleyLayout v2: the venue is a banded, walkable, looping social floor, and it PROVES it rather than claiming it.** v1 was a room with props round the edges; v2 is a stack of Z bands derived from depth fields, so moving one slides everything west of it. West→east: **west band 6.5m** (service spine + casino + card dealer) → **concourse 4.0m** → **settee pits 3.5m** → **ball returns 1.8m** → **approach 4.5m, completely clear**. Final room **30.7 × 41.8m** — still shallower than v1's 42.0m, so the restructure did not lengthen the walk. Six sunken settee pits (one per lane PAIR, 0.4m down, U-bench, table, score console), paired ball returns where a real alley puts them, and front desk / snack bar / bar as three stations on ONE service spine with a continuous staff walkway you can walk into. `GreyboxSceneBuilder.cs` still untouched. **Tony's calls, recorded because each overrode a default:** (1) **Beat A keeps the settee pit in frame** — the turn-start beat looks back at the thrower, so the pit and concourse behind him are on camera for 0.9s every turn; that is the walkable-alley hypothesis sold in one frame, and it stays. (2) **All six pits fully built**, no hero/backdrop split. (3) **No lane-bank barriers** — players may walk down backdrop lanes and that is a feature. (4) **Circulation gets FIVE categories, not three**: primary 4.0m, secondary 2.5m, threshold 1.2m, plus **amenity interior 1.2m** and **back-of-house 1.2m**, both added by Tony after the first audit reported those gaps UNCLASSIFIED and asked him to categorise rather than guess. (5) **The card dealer alcove is EXEMPT from its own minimum at 0.83m** — the brief called it a deliberate dead-end and a good hiding spot; you slip in, you do not stroll in, and widening it to compliance would make it read as just another shop. The exemption and its reasoning live in `CirculationWidthExemptZones` so nobody "fixes" it later. **Open TODO on that one:** verify a player can get in AND back out once characters exist — a squeeze is a joke, two players permanently wedged is a bug. **The safety guarantee got stronger, not weaker:** v1's single collider-exclusion volume was protecting the ball and the camera corridor at once, and that conflation is what made a legitimate settee pit illegal; it split into three checks, one of which (the approach band) now fails on any created object — collider, **renderer or anchor** — because a cosmetic box in the aim camera's face is as bad as a wall. Final audit: **0 errors, 0 unclassified, 11 independent loops, 0 unreachable anchors.** No gameplay, physics, scoring or networking touched.
- 2026-07-26 (later still — pre-Mirror hardening, not a design decision) — Ahead of networking work: added an explicit `LaunchParameters.IsGreen` flag (replacing float-equality on `TimingError01` for the Nuke check), gated every throw-affecting input behind a new `ThrowInputAllowed`/`IsMyAvatar` check anchored on `PlayerAvatar.ControlMode`/`IsLocal` (renamed `isLocalPlayer` when `PlayerAvatar` became a `NetworkBehaviour` in the 2026-08-03 Mirror spike — see this log's 2026-08-04 entries), and split `BowlingGameController` into `BowlingMatchFlow` (host-authoritative match state) + `BowlingPresentation` (client-local camera/input/avatar handoff) so future Mirror work has a clean seam. Full two-class split chosen over a thin internal-only split (Tony's call, weighing the larger one-time diff against not having to redo the separation later); the sandbox `R` key deliberately gates on avatar identity only, not full turn state, since the avatar is already back in Roaming by the time `R` is reachable. No gameplay/feel changes; QA-reviewed, 72 EditMode tests green.
- 2026-07-26 (later — first gameplay decision on the walkable venue, plus real physics bugfixes) — **Roaming + interaction system built (new §7 candidate mechanism), and four real ball/pin physics bugs found and fixed along the way.** Design: `PlayerAvatar` owns a per-player `ControlMode` (Roaming/Bowling), never a global game state — the active thrower locks into the Wii-style stance while everyone else stays free-roaming, which is the actual shape the "walkable alley as a place" hypothesis (§7) needs to be testable and is built so a later Mirror pass makes the OTHER players roam for free rather than requiring a rewrite. `ApplyMode()` is the single chokepoint allowed to enable/disable mode-owned components or touch the cursor. Camera handoff is two cameras (first-person + the existing `ThrowCamera`/`ThrowCameraSequence` stack), switched by disabling `Camera`/`AudioListener` components — never the GameObject — so the throw cinematic's event subscriptions never churn; the Nuke path is untouched. Local model hidden by CAMERA CULLING on a new `LocalPlayerModel` layer, not by disabling renderers, so remote players will see it once Mirror lands. `BowlingGameController` no longer auto-starts (`Start()` split into `Initialize()` + `StartMatch()`); a `sandboxAutoStart` flag (default true) preserves every existing scene's behaviour and keeps throw feel-testing one tickbox away, per Tony's explicit non-negotiable. Match start is now diegetic: a reusable `IInteractable`/`PlayerInteractor` system (registry-based, no trigger colliders) with `LaneKioskInteractable` as its first user, attached directly to the lane's screen object (not a separate marker) so moving the screen moves the hotspot — Tony's call after the first version used a detached anchor marker. Answers `OpenQuestions.md:33`: character controller radius is 0.30m, so a 1.2m circulation threshold is 2.0 body-widths — reported, not acted on. `OpenQuestions.md:22`'s drag-to-the-line comedy transition stays parked; `PlayerAvatar.MoveToThrowingStance` is the one method that will be replaced when it's built.
  **Separately, four physics bugs were found while chasing "the ball isn't hitting pins right":** (1) ball<->pin collision was `CollisionDetectionMode.Continuous`, which only sweeps against STATIC colliders — rigidbody-vs-rigidbody swept collision needs `ContinuousDynamic` on BOTH bodies, so the ball was passing straight through pins at high power with no error; (2) the ball's `angularVelocity` was silently clamped by the project's Default Max Angular Speed (50 rad/s vs. the ~127 rad/s true rolling speed at max power), so it skidded instead of rolling; (3) `PinDeck.ResetFullRack` spawned every pin half-buried in the lane (a pin's origin is its middle, not its foot) and PhysX's depenetration ejection silently ate 0.19m of `IsStanding`'s 0.6m displacement budget on every single rack; (4) pins now use a compound collider — a capsule body on a small flat base pad, replacing one full-size box — because a box presents a flat wall at full width down to the lane, so the ball rode up it instead of glancing off, and a fallen box was a ramp the ball climbed rather than an obstacle it rolled past; centre of mass is now genuinely bottom-heavy (the first attempt at this silently did almost nothing — `Rigidbody.centerOfMass` is local-space and the pin's transform carries a 0.19 non-uniform scale, so a value expressed in metres was being shrunk to a fifth). Ball/pin **mass ratio** was also retuned by feel: real bowling's ~4:1 ball-to-pin ratio read as limp once the collision bugs were fixed and pins were actually being hit — Tony's read was that Cannonball (a deliberately unrealistic ~17:1 ratio) felt right, so the PIN got lighter rather than every ball getting heavier, consistent with this config's existing "lighter pins fly further = funnier" design note. New `WeeSpurts > 1 Assets > Retune Ball + Pin Physics` editor tool stamps coherent values with reasoning attached; speeds, green zones, timing-chaos curves, Nuke staging and Wobbler's weave were untouched — those stay feel, and stay Tony's. QA-reviewed (qa-engineer agent): verdict safe to merge, no hallucinated APIs, no trust/scoring bugs found; five low-risk comment/doc-accuracy fixes applied (this entry is one of them).
- 2026-07-27 (art decision — placeholder character superseded) — **The Quaternius + Mixamo placeholder is replaced by Tony's own Meshy-generated mascot**, via `CharacterSetupTool` repointed at `Meshy_AI_Bowling_Mascot_Rig_biped`'s 11 FBXs — same generator, same Generic-rig approach (`ArtGuide.md`'s "Tony's AI-made characters swap in later via the same humanoid slot" now reads "Generic slot"; rig type not re-tested against Humanoid this session, an open follow-up rather than a ruled-out option). Rig source `Idle_3`; the other 9 clip FBXs `CopyFromOther` onto its Avatar, one take each (Meshy ships one per file, not Mixamo's twelve); `Headache_Relief` split into two named clips (`DrunkIdle` loop, `Defeat` one-shot) from the same take, since the new clip set has no dedicated Defeat animation. **New: the character finally has a throw animation** (`Female_Crouch_Pick_Throw_Forward`) — it previously stood motionless while the ball launched. Sequencing was a feel call Tony confirmed explicitly: `CharacterThrowReactionActor.PlayReaction` fires Throw first and holds the outcome reaction (Excited/Defeat/FallFlat) until Throw's own clip length elapses, rather than firing both the same frame (which would cut Throw off almost immediately — `AnyState` transitions re-evaluate every frame). New `Sprint`/`DrunkWalk` Animator states; `Sprint` needed `FirstPersonController`'s Speed signal uncapped (previously `Clamp01`'d, so sprint and walk were indistinguishable to the Animator) — a presentation-only change to roaming, confirmed low-risk. `CharacterOpacity`/`CharacterDisplayScale` moved from hardcoded generator constants to a new `MascotConfig` ScriptableObject, so Tony can retune both from the Inspector without a code edit or losing the value on the next generator run — the kind of tunable `CodingStandards.md` already says should never have been a constant. One import-pipeline lesson worth keeping: Unity's FBX importer generated `Idle_3`'s embedded material with no texture bound (an internal reference that didn't resolve through Unity's own auto search, discovered by playtest — the mascot rendered as a flat white ghost), so the rig now skips embedded-material generation entirely and the look comes from a transparent copy of Meshy's own shipped, verified-correct material instead. Presentation only — `LaunchParameters`, physics, scoring, the camera stack and the networking gate all untouched.
- 2026-08-04 (bugfix, not a design decision) — **Offline play restored in `TestVenue.unity`/`BowlingAlley.unity` after the Mirror spike turned `PlayerAvatar`/`BowlingMatchFlow`/`BowlingPresentation` into `NetworkBehaviour`s**, without weakening networked behaviour. New `WeeSpurts.Core.NetSession.IsOffline` (`!NetworkClient.active && !NetworkServer.active`, lifted from `BowlingMatchFlow`'s existing throw-path rule) is the one shared place this check lives; `PlayerAvatar.IsThisMachinesPlayer => NetSession.IsOffline || isLocalPlayer` extends it to the player/camera path (`PlayerAvatar` lines 115/197/198/206, `BowlingPresentation.IsMyAvatar`, `PlayerCameraDirector.Apply`) so those sites stop dereferencing a null `netIdentity` when a scene has no `NetworkIdentity`/`NetworkManager` at all. Fail-open ONLY when there is no Mirror session whatsoever — the instant one exists (host, server, or client), this is byte-for-byte `isLocalPlayer`, unchanged. **Flag, not solved here:** the mirror-kcp findings doc's pre-placed-scene-object `sceneId` house rule (hardcode `sceneId` rather than trust Mirror's auto-assignment) becomes relevant to `Player`/`BowlingGame` in these two scenes once Stage E actually networks them — out of scope this session.
- 2026-08-04 (later — doc hygiene pass, not a design decision) — `Docs/Networking.md` updated per the mirror-kcp findings doc's own to-do (`NextSteps.md` item 0, now removed since it's done): added the roaming/continuous-sync section, the pre-placed-scene-object `sceneId` house rule, the editor-vs-build `sceneId` mismatch warning, the Windows Firewall rule, and this session's own offline-fallback rule (previously only recorded here). `Docs/DefinitionOfDone.md` gained a `[4b] Roaming / Player Movement` section — substantially built (`ControlMode`, camera handoff, `PlayerInteractor`) but had no exit criteria of its own until now. `Docs/NextSteps.md`'s Stage E port prompt now explicitly names the offline-fallback fix as a fifth change to carry over — it currently exists only on `spike/mirror-kcp`, a branch that's meant to be deleted after Stage E, so it would otherwise have been silently lost. This log's 2026-07-26 `PlayerAvatar.IsLocal` reference got an inline note pointing at the rename rather than being rewritten, to keep the historical record honest. No gameplay, physics, scoring or networking behaviour touched — documentation only.
- 2026-08-04 (later still — bugfix, not a design decision) — **Second offline bug found by Tony's own playtest: throwing worked once, then the camera froze and the frame never reset.** `BowlingMatchFlow.ResolveThrow`'s `if (!isServer && !IsOffline)` gate (pre-existing spike code, not touched by the earlier entries above) evaluated `isServer` — which reads `netIdentity.isServer` — BEFORE checking `IsOffline`, so on a scene with no `NetworkIdentity` (`BowlingAlley.unity`, `TestVenue.unity`) it threw a `NullReferenceException` and silently killed the coroutine right after logging the local pin count, before `BeginRoll()` could reopen the next aim phase. Same root lesson as the two entries above, just a check the first offline-fallback pass didn't reach: reordered to `if (!IsOffline && !isServer)` so `isServer` is never evaluated while offline. Confirms the original spike author's own comment ("isServer is false with no session running") was an untested assumption — it actually throws, not reads false, with no `NetworkIdentity` present. Behaviour while a session IS running is unchanged (`&&` is commutative when both sides are safe to evaluate).
- 2026-08-04 (later still again — the fake-coin economy exists, as tested pure C#; several decisions recorded) — **`SlopLayerPlan.md`'s F3 + most of S4 built, entirely without touching a scene.** New: `CoinLedger` (Gameplay), `Vendor`/`VendorItem`/`VendorConfig` and `BlackjackTable`/`BlackjackHand`/`PlayingCard`/`BlackjackRules`/`BlackjackConfig` (new `Scripts/Slop/`), `DeterministicRng` and `EconomyConfig` (Core/Gameplay). Built to that doc's three construction rules verbatim: **one choke point** (nothing anywhere writes a balance — the bar, the card table and later betting all call the ledger), **request-then-decide** (there is deliberately no balance setter and no `Spend()` that just does it; the caller asks and reacts to the answer, so the same call sites work unchanged when the decider becomes the host), and **IDs never references** (items, cards and players are all ints/enums that replicate for free). 65 new EditMode tests, **137 green**. **Tony's calls, each recorded because it overrode a default or a doc:** (1) **The ledger is SESSION-scoped, on `GameManager`** (which already survives scene loads), not match-scoped on `BowlingMatchFlow` — so winnings carry between matches like a real alley, and, more load-bearing, *the bar and card table keep working when no match is running*, which is exactly when players wander off to use them. A match-scoped ledger would have made the casino corner dead in the lobby, the opposite of what §7's walkable-alley hypothesis needs. (2) **The card table is blackjack-for-coins**, chosen knowingly over `BowlingFeelIdeas.md` §G's parked powerup-CARDS design — and that entry's explicit sequencing note ("do NOT build before the core throw is proven fun and networking exists") was surfaced and overridden on the grounds that a coin sink is not the same commitment as a chaos layer. **§G's powerup cards remain parked and unbuilt.** (3) **Stock is per-match** (`Vendor.ResetStock`), so the one-golden-hat gag returns next game. (4) **A zero-priced item is refused as a misconfiguration**, not sold as free — a genuine free item needs its own path. **Two corrections to existing code, both cheap now and expensive later:** `PlayerData.Coins` **deleted** — a public balance setter with zero usages, i.e. a second source of truth sitting there waiting for whoever was in a hurry, which is precisely what Rule 1 exists to prevent; and every player id in the new systems moved **`int` → `ulong`** to match `PlayerData.Id`, because that id becomes a 64-bit SteamId and an `int` would have silently truncated at the worst possible moment (a two-house test). **`DeterministicRng` (xorshift32) rather than `System.Random`** for the card shoe: `System.Random`'s algorithm is documented as implementation-defined, and while the spike measured zero drift Mac↔PC on the throw path — which is why `BowlingBall`'s use of it was **deliberately left alone** — a wobble differing by a thousandth is invisible where one different card means two players are looking at different hands. **Deliberate scope limits:** blackjack is one seat, hit and stand only — no double-down, split, insurance or side bets, each being a real money path where a half-built version is worse than none; the slot machine is NOT built; and **none of this is reachable in-game yet.** `BowlingMatchFlow.Initialize` now calls `GameManager.Instance.EnsurePlayer` per player (null-guarded, because `BowlingAlley.unity`'s sandbox has no `GameManager` and the throw path must never depend on the slop layer), but nothing instantiates a `Vendor` or a `BlackjackTable` in any scene. **Flagged, not solved:** walking up to the card table needs a third `ControlMode` (`Seated`), which means editing `PlayerAvatar.ApplyMode` — the single chokepoint whose whole value is that nobody edits it casually — so it was left for a session of its own with Tony's explicit sign-off. No scene, prefab, physics, scoring or networking behaviour touched.
- 2026-08-11 (direction change — Tony's call, several earlier entries superseded) — **The currency is TICKETS, and the open question is closed.** `Docs/Projects.md`'s "Currency name: still open" is answered: there is one currency, it is called tickets, and the fiction is the arcade/alley ticket. Tickets buy everything — drinks, cosmetics, arcade plays, blackjack stakes, slot pulls. `CoinLedger` and its family rename to `TicketLedger`/`TicketTransaction`/`TicketResult`; the one-choke-point, request-then-decide and ids-never-references construction rules carry over unchanged, because those are about code correctness rather than fiction. **`ControlMode.Seated` is built** — the 2026-08-04 entry's "flagged, not solved" item is closed, and the casino nook is reachable. **New systems committed to:** a real **inventory** (per-player slots, ids from an `ItemCatalog`, `Throwable`/`LaneAction`/`Instant` use contexts, filled from the ball return AND from vendors); **lane conditioning** as a sampled field down the lane that `BowlingBall` reads when computing hook, laid and cleared by items, decaying naturally as balls track through it; and **lane sabotage** — the janitorial set (oil can, rosin, floor wax, mop, loose pin, and a wet floor sign with no effect at all so it can be used as a bluff), usable on the second throw of a frame, which means a strike ends the frame and therefore disarms you. The mop is usable by **spectators**, not only the player being sabotaged. **Process note, recorded deliberately:** Tony's direction changes as the game gets designed, and this doc exists to record where it ends up, not to argue with him about where it started. Where a prompt or a newer doc contradicts an older entry here, the newer one wins and this log records the change. `Docs/Prompts/2026-08-11-venue-economy-buildout.md` is the current build order. Design constraints that were previously written as standing laws — expected-value rules across the casino and lane, "the room orbits the lane", parody-not-simulation, and the age-ratings analysis — are **withdrawn as constraints**; Tony reviews the shape of the game near the end rather than building against them up front.
- 2026-08-11 (later — mechanical rename, Stage 1 of the buildout prompt, not a design decision) — **The coin→ticket rename above is executed in code.** `CoinLedger`/`CoinTransaction`/`CoinResult` → `TicketLedger`/`TicketTransaction`/`TicketResult` (file + `.meta` renamed together so Unity kept the GUID); `EconomyConfig.StartingCoins`/`DEFAULT_STARTING_COINS` → `StartingTickets`/`DEFAULT_STARTING_TICKETS`; `GameManager.Coins`/`StartingCoins` → `Tickets`/`StartingTickets`; `Vendor`'s `PurchaseOutcome.NotEnoughCoins` → `NotEnoughTickets`; every call site in `BlackjackTable` and every doc-comment/tooltip mentioning "coin" across `Scripts/Gameplay`, `Scripts/Slop` and `Scripts/Interaction` updated to "ticket". Tests renamed and updated to match (`CoinLedgerTests` → `TicketLedgerTests`, `VendorTests`, `BlackjackTests`). `BowlingMatchFlow.cs` needed no change — it only ever called `GameManager.Instance.EnsurePlayer`, never named the ledger directly. `Scenes/ThunderLanesVenue.unity`'s `Coin_Tray`/`Coin_Token_Dispenser` GameObjects are physical arcade-prop meshes, unrelated to this ledger, and were left alone — no scene YAML touched. Historical entries above (e.g. 2026-08-04's `CoinLedger` build) are deliberately left saying "coin", as the record of what was true then.
- 2026-08-11 (later still — Stage 2 of the buildout prompt) — **The economy is reachable in-game for the first time.** New `Slop/VenueStation` — an abstract `IInteractable` base modelled on `LaneKioskInteractable`, owning only registration/interaction-point plumbing, never balances — with two concrete stations: `BarStation` (drives a `Vendor` built from a `VendorConfig`, one item per station rather than a menu, since no shop UI exists yet) and `BlackjackStation` (sits the player via the existing `ControlMode.Seated`, deals a `BlackjackTable` hand at a fixed stake). **Seated input is NOT routed through `IInteractable`** — `PlayerAvatar.ApplyMode` turns `PlayerInteractor` off for `Seated` on purpose (same reason it's off for `Bowling`), so `BlackjackStation` reads its own seated player's Hit/Stand/leave keys directly in `Update`, same shape as `ThrowerAimSlide` owning input during a throw; its `OnGUI` hand readout is a deliberate DebugHud-style stand-in, not real UI. New `PlayerAvatar.EconomyPlayerId` (ulong, offline-safe wrapper around Mirror's `netId`, same `NetSession.IsOffline` guard as `IsThisMachinesPlayer`) is what a station hands the ledger — the roaming avatar had no economy identity before this. New `ThunderLanesVenueStationSetupTool` (idempotent, menu-driven, matches `ThunderLanesVenueRoamingSetupTool`'s dirty-not-saved precedent) places both stations by MEASURING existing art (`Bar_and_Lounge`, `Blackjack_Pit_Table`, `Dealer_Podium`, floor meshes) rather than hardcoding coordinates, skipping a station with a loud warning if its anchor art is missing rather than guessing; it also creates the scene's `GameManager` if none exists and adds a `TicketBalanceHud` (top-right ticket count, same OnGUI-debug philosophy as `DebugHud`) so a purchase is actually visible. No scene or prefab YAML hand-edited — all scene changes go through the new tool. **NOW TEST:** run `WeeSpurts > Thunder Lanes Venue > Set Up Economy Stations`, press Play, walk to the bar and buy a drink (ticket count drops top-right), then walk to the casino nook, sit at the blackjack table, and play a hand out with SPACE/ENTER/ESC.
- 2026-08-11 (later still — executive-day override, Block 1 task 2 of `Docs/Prompts/2026-08-11-executive-day-plan.md`) — **Blackjack gets double-down and splits, overriding the earlier "hit/stand only, no splits" scope in `Docs/OpenQuestions.md` §Blackjack and `Docs/Projects.md` §C.** Those docs' argument was never that splits are bad blackjack — it's that a longer hand pulls attention away from the lane, which the docs call the spine of the game. Tony's call, recorded rather than argued: build it, and watch the first four-player test for a casino nook that goes quiet and stays quiet while someone's bowling; if that happens the fix is a hand timer, not removing splits. **`BlackjackTable` restructured to support multiple player hands**: `PlayerHands` (indexed, always ≥1) + `ActiveHandIndex` replace the single `PlayerHand` field, which now survives only as a convenience alias for the active hand so `BlackjackStation`'s existing OnGUI/Update code needed no changes. `Double(ledger)` is legal on a hand's first two cards only, takes a second stake equal to that hand's own, deals exactly one card, and auto-stands. `Split(ledger)` is legal once per round (no re-splitting, which is what keeps the state machine finite regardless of how the shoe deals) on a matched RANK pair, takes a second stake, and plays the two hands in strict sequence. **Aces split once, one card each, then auto-resolve with no further action** — standard casual rule. **The 3:2 natural bonus is now structurally impossible to pay on a post-split hand**: it is checked in exactly one place, at the deal, before a split is even possible, so any hand that reaches 21 after being played (hit, doubled, or split) settles at even money through the same total-comparison path `Stand()` always used — this is also the textbook-correct real-world rule, not a house-favoring shortcut invented here. `BlackjackRound` (unchanged struct) is now produced once per hand and exposed as `LastRoundResults` (one entry normally, two after a split); `LastRound` stays as an alias for the first hand's result. `DealerHitsSoft17` and every existing payout number are untouched. 10 new EditMode tests in `BlackjackTests.cs` (double/split legality, stake-taking, aces, two-hand settlement, a hand busting while its split partner wins, and `TotalInCirculation` moving by exactly the combined net of both hands) — suite green. No scene, prefab, or UI work; `BlackjackStation.cs`'s debug Hit/Stand controls are unchanged, so Double/Split are not reachable in-game yet (that's Block 8's job).
- 2026-08-11 (later still — Stage 3 of the buildout prompt) — **The inventory backbone exists, pure C#, no scene work.** New `Gameplay/Inventory` — fixed slots per player, each holding an item id and a count — built to the exact same three rules `TicketLedger` follows: one choke point (`TryAdd`/`Remove` are the only ways a slot changes), request-then-decide (a refusal is returned, never thrown), and ids never references. **`TryAdd`/`Remove` are ALL-OR-NOTHING**, matching `TicketLedger.Transfer`'s atomicity — a dry-run capacity pass runs before anything is touched, so an add that would only partially fit refuses entirely rather than leaving a partial stack behind. `Inventory` holds its OWN copy of the catalog (a deduped `List<InventoryItem>`, first-id-wins on a duplicate) rather than a reference to the new `Gameplay/ItemCatalog` ScriptableObject — same DATA/LOGIC split as `VendorConfig`/`Vendor`, and what keeps this class touching zero `UnityEngine` types. `ItemCatalog.CreateInventory(slotCount)` is the bridge, same role as `VendorConfig.CreateVendor`/`BlackjackConfig.CreateRules`; slot count stays a parameter rather than a catalog field because it's a per-player decision, not a shared one. New `ItemUseContext` enum (`Throwable`/`LaneAction`/`Instant`) is on `InventoryItem` now so the catalog's shape is right ahead of Stage 4, but nothing reads it yet. `VendorItem` gained `InventoryItemId` (-1 default = none) so a vendor item can carry an inventory id — **explicitly NOT wired up**: no scene code deposits it anywhere yet, because nothing owns a per-player `Inventory` instance until a later stage decides where one lives (the roaming avatar? the session, like `TicketLedger`? left open on purpose rather than guessed). 20 new EditMode tests (`InventoryTests`), covering add/remove/stacking/non-stacking/overfill/atomicity/unknown-id/events, suite green. No scene, prefab, or ledger behaviour touched.
- 2026-08-11 (later still — Block 1 tasks 1, 3 and 4 of `Docs/Prompts/2026-08-11-executive-day-plan.md`, the unattended run) — **Three more pure-C# systems landed in one unattended session while Tony was on Mixamo: the slot machine engine, the drink meter model, and the inventory item-loop plumbing.** All three are engine-only — no scene, prefab, `.controller`, `.meta` or importer work, per that session's explicit override of `CLAUDE.md`'s one-system-per-session rule (pure C# with EditMode coverage only). Suite went from 157 to 224 tests, all green throughout; the split-round money-invariant test caught one real bug in ITS OWN test setup (a baseline balance snapshot taken after the deal's stake instead of before it, double-counting that stake) before landing, which is exactly the kind of thing this style of testing exists to catch.
  **1 — `Slop/SlotMachine`, the Thunder Lanes house machine**, built to mirror `BlackjackTable`'s shape exactly: `SlotConfig` (ScriptableObject, data) / `SlotRules` (plain C#, the engine's actual input) / `SlotMachine` (the engine) split, same as `BlackjackConfig`/`BlackjackRules`/`BlackjackTable`. Three reels, six symbols (pin, beer, bowling ball, bowling shoe, ticket, mascot face), each reel drawn independently from its own weighted strip — **volatility lives entirely in the reel strips, not in special-cased probability code**: the default strip is 3 pin / 6 beer / 5 ball / 5 shoe / 4 ticket / **1 mascot** per 24 slots, so the mascot jackpot (250x, paytable's biggest number) is naturally rare (≈1 in 13,800 pulls) without the payout math knowing anything about rarity. **Three pins trigger FREE FRAME** instead of a cash payout: `BonusPullsRemaining`/`BonusMultiplier` live as state on the machine itself and the next N calls to `Pull()` are transparently free and multiplied, so the station layer that will eventually drive this in-scene never needs to know the machine is mid-bonus — same "dumb caller" shape as `BlackjackTable.Deal`/`Hit`/`Stand`. A retrigger mid-bonus stacks additional free pulls rather than resetting them. 18 new EditMode tests including the required 100k-pull payout-distribution test, which measures realised RTP at **~46.9%** with a >95% no-match rate ("mostly nothing") — comfortably inside a deliberately wide assertion band chosen to catch a broken paytable (0% or >100% RTP) without being a flaky test on a fat-tailed distribution. QA-reviewed (qa-engineer agent) for ledger/seed correctness before being called done, per that session's High-QA flag.
  **2 — `Gameplay/DrinkMeter`**, pure C#, deliberately ignorant of the ball, the lane, and the Animator — it is, on purpose, "a number and a curve." A 0..1 `Level` with `Drink(amount)` (ignores non-positive amounts — this is not a way to sober up) and `Decay(deltaFrames)` (caller owns the frames-vs-seconds conversion), four named tiers (Sober/Loose/Drunk/Gone) off configurable thresholds, and three read-only outputs for the aim phase to consume later: `PowerMeterSpeedMultiplier` and `GreenZoneWidthMultiplier` both **fall** toward a configured floor as `Level` rises, `AimSwayAmplitude` **rises** toward a configured ceiling — this is the "slower meter helps, narrower window is the cost" trade Tony called for (recorded elsewhere in this log), expressed here as two independent linear curves so he can retune the balance in `DrinkConfig` by playing rather than by reasoning about it. `DrinkTunables.Sanitize()` forces the two floor multipliers into `(0, 1]` (so a config typo can't make drinking make you BETTER) and forces the three tier thresholds into non-decreasing order. Not wired to anything yet — `BarStation` granting drinks and the aim-phase systems reading these outputs are both later work.
  **3 — `Gameplay/Inventory` additions for the item loop**: `Drop(itemId, amount)` (same all-or-nothing shape and return type as the existing `Remove`) plus `FirstNonEmptySlot()`/`NextNonEmptySlot(from)`/`PrevNonEmptySlot(from)` so a HUD can cycle without reaching into the slot array directly — each is a bounded scan (at most `SlotCount` steps), never a `while(true)`, so an empty or single-item inventory cannot hang. New `HeldSlotIndex` (-1 = holding nothing, the starting state) with `SelectSlot(int)` (a direct HUD click, which MAY land on an empty slot — that's a legal "hand empty, this slot highlighted" state), `CycleNext()`/`CycledPrev()` (which, unlike `SelectSlot`, always skip empty slots and wrap), and a new `OnHeldChanged` event that only fires on an actual change, never on a no-op re-selection. `Drop` specifically checks whether the slot it just emptied was the one currently held, and if so moves the hold to the next non-empty slot (or to -1 if nothing's left) — dropping an item you AREN'T holding never touches `HeldSlotIndex`. 21 new EditMode tests covering cycling across empty gaps, cycling an empty or single-item inventory, and the held-slot-updates-on-drop behaviour in each of its cases (emptied/not-emptied, held/not-held, last-item-vs-not).
- 2026-08-11 (later still — QA pass on the two High-QA items above, `qa-engineer` agent) — **Two agent reviews found four real bugs pre-merge, none money-unsafe, all fixed.** Blackjack: after any split that isn't a full ace-auto-resolve, `ActiveHandIndex` was left parked on whichever hand played last, so `PlayerHand` (hand 1's cards) and `LastRound` (hardcoded to hand 0's result) would show a mismatched pair once a HUD reads both together — not reachable today (Double/Split have no key binding yet) but would have bitten Block 8. Fixed by resetting `ActiveHandIndex = 0` at the end of both settlement paths (`SettleNatural`/`SettleAllHands`). Slots: (1) `Math.Clamp(int,int,int)` needs .NET Standard 2.1; this project's `ProjectSettings.asset` targets 2.0, so it could have compiled fine in the Editor/Test Runner while failing an actual player build — replaced with a manual clamp matching `TicketLedger`'s own pattern for pure-C# classes that can't use `Mathf` either. (2) `SlotPull.Net` returned `-Stake` on a refused pull (nothing was actually charged) instead of `0`, a trap for any future HUD that trusts `.Net` without checking `.Spun` first — fixed to read `0` whenever the pull didn't actually spin. (3) The payout multiplication (`stake * paytableMultiplier * bonusMultiplier`) had no overflow guard — an Inspector-tuned `MaxStake` or paytable multiplier far past today's placeholder numbers could wrap `int` negative and silently skip payment, or wrap positive and pay the wrong amount; fixed with a `long` intermediate clamped to `int.MaxValue` before casting. Two regression tests added for the Slots fixes. Suite: 224 → 226, still green.
- 2026-08-11 (later still — Block 2, the Humanoid rig conversion, part 1 of 2) — **The mascot is now a Humanoid rig, so Mixamo clips can retarget onto him.** This reverses a decision recorded earlier in this log, and the reversal is narrower than it looks: the old "why Generic, not Humanoid" argument in `CharacterSetupTool` was about the **Quaternius** bodies, and it is still correct about them — they export a Blender IK *control* rig (`Foot.L`/`Foot.R` parented to root as IK targets, siblings of the leg chain rather than children of `LowerLeg`), Unity's Humanoid validates hierarchy and not just names, so they genuinely cannot pass. Those bodies are still explicitly pinned to Generic. The **Meshy** mascot was always a separate, unanswered question — that comment said so — and reading the FBX answers it: its bones are `Hips / Spine / Spine01 / Spine02 / LeftShoulder / LeftArm / LeftForeArm / LeftHand / LeftUpLeg / LeftLeg / LeftFoot / LeftToeBase / neck / Head`, clean Mixamo-convention FK naming with no IK targets and no fingers (fingers are optional to Unity). **The point of the change is retargeting**: Humanoid stores a clip in normalised muscle space, so stock animation authored on any humanoid skeleton plays on him — which is what lifts the character out of the ten takes Meshy generated. Per `Docs/Prompts/2026-08-11-executive-day-plan.md` §1 this is also expected to *reduce* the "gooey arms" read by separating the arms from the torso, but not to fix it: the cause is skin-weight bleed at the shoulder in Meshy's auto-rig, and that needs a re-skin or a different mesh, not an animator.
  **The trap, recorded because it is silent and someone will hit it again:** the two clip sources need *different* avatar setups, and the only question that matters is "same skeleton?". Meshy clips came out of the same export as the rig, so their bone names match it and `CopyFromOther` against the mascot's avatar is correct. Mixamo clips ship their *own* skeleton named `mixamorig:Hips`, `mixamorig:LeftArm`, … so `CopyFromOther` would apply the mascot's bone-name mapping to a rig that has none of those names, bind nothing, and produce a **T-pose with no import error at all**. They must be `CreateFromThisModel`, letting each file build its own humanoid avatar, with retargeting doing the rest at runtime. The day plan's own prompt specified `CopyFromOther` for both; that was corrected before writing, on Tony's go-ahead.
  **Also settled: root motion.** `lockRootPositionXZ`/`lockRootHeightY`/`lockRootRotation` are "Bake Into Pose" (does the bake happen); `keepOriginalPositionXZ`/`keepOriginalPositionY`/`keepOriginalOrientation` are "Based Upon" (what frame it's measured against) — different things with confusable names. Every clip is now baked in-place, because the game is `CharacterController`-driven and the scene owns position. Y is baked but measured from the clip's *original* height so his feet stay on the floor; XZ and rotation are measured from centre of mass / body orientation. This is **not** a behaviour change smuggled in with the conversion — Root Transform settings are Humanoid-only and were simply inert while the clips were Generic, so leaving them at defaults is what *would* have changed behaviour (`applyRootMotion = false` discards root motion rather than preventing its extraction, which reads as foot-sliding).
  New `ReportHumanoidMapping` prints avatar `isValid`/`isHuman` and every unmapped human bone, splitting Unity-required from Unity-optional, and **hard-stops the tool** when a required one is missing rather than building a prefab that would silently T-pose. It drives off the `HumanBodyBones` enum via `Animator.GetBoneTransform` rather than off bone-name strings, so no naming convention can break it, and it refuses to guess a mapping — `Rig > Configure` is a human's job. `optimizeGameObjects` is now pinned off, because stripping bone Transforms would break both that check and the carried-item work in Block 6.
  **Deliberately still broken, part 2 of 2:** `DrunkIdle` and `Defeat` remain two clips carved from the same `Headache_Relief` take (he rubs his temples both when drunk and when he loses), and `SitIdle`/`StandToSit`/`Stumble` do not exist. Meshy shipped no slump and no sway, so there is nothing in the project to point them at — the clip table needs the Mixamo downloads, which were not on disk this session. Not patched with a Meshy substitute in the meantime, because that hides the symptom without fixing it. Note the six FBXs sitting in `Animations/` are **not** usable for this: they are old Mixamo downloads retargeted onto the *Quaternius* skeleton (`Foot.L`, `UpperArm.L`, `MiddleHand.L`), a different skeleton from the mascot. `MascotConfig.DisplayScale` was re-measured, not retuned: `LogCharacterHeight` now also prints the scale that *would* hit 1.75 m and leaves the dial alone. Worth knowing the shipped asset reads `0.7`, not the `0.56` in the code default — create-once means Tony's Inspector tweak survived — which puts him around 2.2 m.
- 2026-08-11 (later still — new `Docs/CharacterPipeline.md`, and the mascot body swapped again) — **The "gooey arms" root cause named:** the old mascot was generated arms-down, so every auto-weighting algorithm (Meshy's, Mixamo's, Blender's) couldn't tell an armpit vertex from a chest vertex and bound both to the same bone — a mesh problem baked in at generation time, not a rigging problem any re-rig could fix. `CharacterPipeline.md` records the fix (regenerate in a T-pose, arms clearly clear of the torso) and the four-layer architecture meant to make this the *last* time a character swap hurts: **Layer 1** skeleton (Humanoid, forever), **Layer 2** body mesh (swappable, one path constant in `CharacterSetupTool`), **Layer 3** attachments (bone-fetched by `HumanBodyBones` enum, never by name), **Layer 4** look (`MaterialPropertyBlock`, never a new material instance). `CharacterModelPath` now points at `Meshy_AI_Character_output.fbx` (T-posed regeneration), verified by reading the FBX directly rather than trusting the export notes: 5208 verts / 10,408 tris, 24 bones + an `Armature` null (same Mixamo-convention FK naming as the old rig, **plus real toes this time**, still no fingers, no blendshapes), one take (`Armature|Armature|clip0|baselayer`), one embedded texture. Verified again post-import in an isolated throwaway Unity project (batchmode, so as not to fight Tony's already-open editor instances): avatar `isValid=isHuman=True`, **all 15 required Humanoid bones mapped, 0 missing** (the 33 unmapped optional bones are exactly fingers/eyes/jaw, expected), raw unscaled height 2.44 m (`DisplayScale` 0.72 would land him at the 1.75 m target). The **old mascot is deliberately left in place** as the clip source — all eleven of its FBXs (`CopyFromOther` onto the new rig's Avatar) are unaffected, since Humanoid retargeting is bone-*name*-agnostic by construction — and as a one-constant-swap fallback (`OldMascotModelPath`) until Tony confirms the new body actually reads better in motion. One importer wrinkle the swap exposed: the old mascot's material had to bypass the FBX importer entirely (`materialImportMode = None` + Meshy's own shipped `.mat`, because its texture reference didn't resolve through Unity's auto-search); the new mascot embeds its texture *inside* the FBX instead, so `None` would have discarded the only copy of it — it gets standard import + the ordinary transparency remap every Quaternius body already used (`RigShipsItsOwnMaterialAsset` picks between the two per-file rather than assuming one FBX shape for both).
- 2026-08-11 (same session — Layer 3 built: `Characters/AttachmentState.cs`, `AttachmentCatalog.cs`, `CosmeticAttachment.cs`, `AttachmentSlots.cs`) — **Hats and held items are one system now, and Block 6's ball goes through it.** `CosmeticAttachment.Attach(Animator, AttachmentDefinition)` instantiates a prefab and parents it to `Animator.GetBoneTransform(HumanBodyBones)` — bone-*name*-agnostic by construction, so it survives any future Layer 2 body swap. `AttachmentCatalog` is data (int id → prefab/bone/offset), same data/logic split as `ItemCatalog`. `AttachmentSlots` is the logic: `Equip(slot, itemId)`/`Unequip(slot)` against a five-slot enum (Head/Face/RightHand/LeftHand/Back), one item per slot, equipping always tears down whatever was there first. State is `AttachmentState` — five plain ints, not a list of object references, specifically so it can become a Mirror `[SyncVar]` later with **zero call-site changes**: every mutation already goes through `With`/`ApplyState`, which is exactly the shape a sync hook needs. Built and tested (`AttachmentCatalogTests`, `AttachmentStateTests`, EditMode) against a code-generated placeholder cube (`AttachmentSetupTool`, magenta, no collider) rather than the real cosmetics, which were still 2M-tri Meshy exports awaiting re-export at the time this system was built.
- 2026-08-11 (same session, later — the re-export landed; first real `AttachmentCatalog`/`ItemCatalog` rows) — **Bowling_Ball, Bowling_Pin, Bowling_Pin_Hat and Mop came back from Meshy low-poly** (838/1230/1559/1253 tris — checked by reading the `.glb` binaries directly, not taken on faith) and are wired in: **Bowling Ball → `AttachmentCatalog` id 1, `RightHand`** — the same prefab Block 6's carried-item work should equip, not a parallel mechanism. **Bowling Pin Hat → id 2, `Head`**, the first real row, placeholder cube retired from those two ids. **Mop → `ItemCatalog` id 1, `LaneAction`**; **Bowling Pin ("Loose Pin") → id 2, `Throwable`**, its mesh also reusable as a venue prop (drag-in, not scripted — scenes are a hand job per `CLAUDE.md`). Real ids start at 1 in both catalogs; `AttachmentSetupTool`'s still-pending placeholders (Crown, Party Cone Hat, Propeller Cap, Beer Bottle — not yet re-exported) were moved to a 901+ band specifically so a real item landing later never has to renumber one out of the way. Both catalog-building tools are now **add-if-missing, never overwrite**, so running either again can't stomp a Tony hand-edit or the other tool's rows. **New standing rule, `ArtGuide.md`:** prop textures max 1024 as committed source, 512 compressed-with-mipmaps in-engine, no metallic/roughness on stylized props — enforced automatically for anything under `Art/Props/` by `PropTexturePostprocessor` (`OnPreprocessTexture`) rather than left to memory. The mechanism was non-obvious enough to write down: glTFast (`com.unity.cloud.gltfast`, confirmed by reading its importer source) decodes embedded glTF textures straight into importer sub-assets with **no** `TextureImporter` behind them at all — no Max Size, no Compression — so getting an actually-capped prop texture meant extracting each BaseColor PNG to a standalone file and pointing a hand-built material at *that* instead of glTFast's own, the same "don't trust the generated one, build your own" move `CharacterSetupTool` already made for the mascot's material. MetallicRoughness maps were extracted from none of the four — not wired to anything — per the flat-stylized direction; still sitting untouched inside the source `.glb` files pending Tony's go-ahead to strip them for real.
- 2026-08-12 (Block 8 of `Docs/Prompts/2026-08-11-executive-day-plan.md`, a day late — blackjack and slots are playable now, not debug keys) — **The casino nook gets real uGUI, and slots exist in the venue for the first time.** Block 1 (2026-08-11) built `BlackjackTable.Double`/`Split` and the `SlotMachine` engine entirely headless; neither had ever been reachable as anything but a debug `OnGUI` box (blackjack: Space=Hit/Enter=Stand/Esc=Leave, bet hardcoded to `config.MinBet`, no Double/Split key at all) and slots had NO scene presence whatsoever. Both are real now, driven by Tony's direct ask.
  **1 — the project's FIRST real uGUI Canvas.** Every HUD before this (`DebugHud`, `TicketBalanceHud`, the old blackjack box) was `OnGUI` immediate-mode, explicitly labelled "placeholder" in each one. Chose real Canvas over more `OnGUI` because `Docs/UI.md`'s 🔒-locked "every interactive element must have a controller-navigable path" has no `OnGUI` equivalent — uGUI's Selectable navigation does, and "big, loud, chunky" reads better as styled Buttons than grey boxes. Built entirely by an EXTENDED `ThunderLanesVenueStationSetupTool` (same idempotent get-or-create discipline as every station before it — Canvas/Button/Slider/LayoutGroup trees constructed procedurally in C#, nothing hand-edited in scene YAML), plus a new `EnsureEventSystem` step (`StandaloneInputModule`, matching `ProjectSettings.activeInputHandler: 2` — without an EventSystem no uGUI Button/Slider in the scene receives a click at all). Needed `Unity.TextMeshPro` added explicitly to both `WeeSpurts.Runtime.asmdef` and `WeeSpurts.Editor.asmdef` — package assembly references aren't transitive or auto-included once a project has custom asmdefs, unlike `UnityEngine.UI` which came for free.
  **2 — `BlackjackStation`/`UI/BlackjackHud`.** A bet slider (drag, clamped to `config.MinBet..MaxBet`) + Deal button replace the fixed-stake auto-deal; Hit/Stand/Double/Split/Leave are real buttons; Double/Split grey out via new `BlackjackStation.CanDouble`/`CanSplit`. Cards are procedural placeholders (no atlas exists yet) — small coloured panels with rank+suit text, red for Hearts/Diamonds — built and torn down at RUNTIME by the HUD itself, because hand size varies with hits and hand count varies with a split, so the setup tool only ever builds the static skeleton (hand containers, not cards). A split's two hands render side-by-side with whichever is `ActiveHandIndex` highlighted, so a split table doesn't read as two identical hands.
  **3 — new `Slop/SlotStation`, `UI/SlotHud`.** Third `VenueStation`, same shape as `BlackjackStation` on purpose. The venue already had three `SlotMachine_N` art objects with no station on them — each now gets a `Station_Slot_N`, seated at the nearest unclaimed `Casino_Stool` (falls back to standing in front of the machine with a loud warning if none is free), sharing one `CasinoSlotConfig` asset (new) with a **per-machine seed offset** so the three don't roll identical sequences. Reels flicker through random symbols before settling on the real `SlotStation.Pull()` result — cosmetic only, so it uses plain `UnityEngine.Random`, never the seeded engine RNG, same rule this project already applies to non-throw-driven randomness. `FREE FRAME` gets its own banner and locks the bet slider (a free pull plays at whatever bet earned it, per `SlotMachine`'s own contract).
  **4 — QA finding, fixed before landing.** `BlackjackTable.CanDouble`/`CanSplit` (new this session) check every legality rule `Double()`/`Split()` enforce EXCEPT affordability — the table holds no `TicketLedger` by design (its own class comment: pure C#, no economy dependency), so it structurally cannot. Left exactly as that limitation at the table layer (documented in its doc comment, pinned by `BlackjackTests.CanDouble_DoesNotCheckAffordability_ThatIsTheStationsJob` so nobody "fixes" it into a ledger dependency later) and fixed one layer up instead: new `BlackjackTable.ActiveHandStake` plus `BlackjackStation.CanDouble`/`CanSplit`, which additionally check the seated player's balance and are what the HUD actually reads. Without this fix a broke player would have seen an enabled Double/Split button that silently did nothing on click — not a money-safety bug (no double-spend), but a real one.
  **5 — a bug of my own, caught before Tony would have hit it.** The runtime-built card visuals have no `LayoutElement`; the hand container's `HorizontalLayoutGroup` defaults `childControlWidth`/`childControlHeight` to `true`, which would have driven every card's size from a (missing) `ILayoutElement` and collapsed them to 0×0 — invisible cards. Fixed by setting both false on that one container (cards set their own size directly at runtime); confirmed in the saved scene YAML, not just in source.
  **Explicit scope cuts, recorded so they read as decisions, not oversights:** no multi-seat blackjack (needs the `Seat` component from the still-unbuilt Block 4 sit-anywhere pass); no dealer NPC; no 3D card meshes, chip stacks or spinning 3D reels (2D Canvas only — no art exists yet, and none of this blocks swapping real art in later); no audio (Block 5's `AudioManager` growth never happened either — still a 35-line stub with no clip catalog).
  246 EditMode tests, suite green throughout (245 baseline + 1: the affordability-gap pin). Verified end-to-end in batchmode, not just compiled: `ThunderLanesVenueStationSetupTool.RunAndSave` run twice against the live scene with no errors (3/3 slot machines got a station, blackjack Canvas built and wired), confirming the tool's own generated report before Tony ever has to press Play.
  **6 — three more bugs, all found by Tony actually pressing Play (the thing no batchmode run can substitute for), all fixed same-day.** (a) Every row (`BetRow`, `ActionGroup`, etc.) had BOTH a `ContentSizeFitter` self-controlling its height AND a parent `VerticalLayoutGroup` externally controlling that same height — two Unity layout controllers fighting over one axis in the same rebuild pass, undefined order, and it collapsed every row (so every button and slider inside it) to zero height. First screenshot: a dark panel with labels but no buttons at all. Fixed by dropping the `ContentSizeFitter` — `HorizontalLayoutGroup` already reports a correct preferred size to its own parent by itself. (b) Every button/slider/reel-slot color used `if (color.alpha == 0f)` as an "only set this on first creation" guard — but a freshly-added `Image` component's real default is OPAQUE WHITE, not transparent, so that guard never fired and no custom color was ever actually applied. Combined with white button-label text, this was invisible-on-invisible: second screenshot showed plain white rectangles where DEAL/HIT/PULL should read. Fixed by making every one of those assignments unconditional (nothing has been hand-restyled yet, so the guard was solving a problem that didn't exist). Buttons now green, `Leave` buttons a distinct muted maroon so quitting doesn't read as just another equal-weight choice, slider fills gold — all spot-checked against the actual saved scene YAML, not the tool's own log, after the first fix attempt turned out to have been silently re-running the STALE pre-recompile DLL (a real Unity gotcha: re-running an `-executeMethod` too soon after a script edit executes the OLD compiled code with no warning). (c) Even after (a) and (b) landed, Tony still couldn't click anything — `PlayerAvatar.ApplyMode` locks and hides the cursor while `Seated`, for mouse-look, a policy that predates any clickable UI existing at a seated station (the old blackjack table only ever read raw keyboard input). `Seated` now matches `Bowling`'s existing policy instead (camera control off, cursor free and visible) — the exact same trade `Bowling` already made for `SpinSelectorHud`'s own click-and-drag widget. No seated station has ever needed to look around independently of its own UI, so nothing that currently exists loses anything; a future station that DID want seated free-look would need its own carve-out. Changed in `PlayerAvatar.ApplyMode`/`ApplyCursor`, not in either HUD — that class's own doc comment is explicit that it is "THE ONLY PLACE mode-owned components and the cursor are switched," and that discipline is exactly what made this a five-line fix instead of a hunt through two HUD scripts for a rogue `Cursor.lockState` write.
  **7 — same day, Tony's next round of feedback: a real bug plus "make it feel like a casino."** Bug: standing on a hand and dealing again left the OLD hand's cards on screen until the next Hit. Root cause — `BlackjackHud`'s dirty-check only compared card COUNT, and a fresh 2-card deal after a 2-card stand never changes count, so it never rebuilt. Fixed by tracking each hand by CONTENT (`PlayingCard.ToString()` per card, e.g. "AS"/"10H") instead of count — a same-count-different-content hand is now correctly detected as changed, and a same-count-same-content hand correctly does nothing (no wasted rebuilds). Feature: cards now deal with a stagger (`BlackjackHud.dealStagger`, default 0.18s between cards) and a scale-in "landing" animation (`cardLandDuration`, 0.15s) — applies to a fresh deal (all cards staggered from index 0) and to a Hit (just the one new card, detected as an append when the existing hand is an exact prefix of the new one). Both Tony-tunable in the Inspector, no code edit needed to retime the feel.
  **8 — the HUD layout itself, redone toward "gamble with friends" reference art Tony sent** (a phone casino game: a barely-there view of the felt with cards, a chunky rounded control bar low in frame). Split the single centred modal panel into two docked pieces — `TableArea` (top-anchored, low-alpha `{a: 0.4}` background so the actual 3D table stays the visual focus, holds dealer/hand rows and the outcome line) and `ControlBar` (bottom-anchored, solid, holds bet/action buttons and Leave) — via new `PanelDock`/`EnsureDockedPanel`. Buttons, the bet slider, and both panels now use Unity's own built-in rounded UI sprites (`AssetDatabase.GetBuiltinExtraResource<Sprite>("UI/Skin/...")` — the exact resources `GameObject > UI > Button` uses, so this cost no art and no import settings) instead of flat rectangles. **This is explicitly the achievable-now slice, not the full ask**: cards are still 2D screen-space UI, not physical objects landing on the actual table mesh in 3D. That's real, separate follow-on work (world-space card placement anchored to `Blackjack_Pit_Table`'s own transform, camera-relative sizing) — flagged here rather than attempted blind in the same pass that was already this deep in layout debugging.
  **9 — the layout debugging under (8), recorded because the wrong assumption cost real time and the right one is worth keeping.** Went in assuming a fresh `HorizontalOrVerticalLayoutGroup`'s `childControlWidth`/`childControlHeight` default to `true` (Unity's docs read that way at a skim) — **confirmed false, by reading the actual saved scene**: they default to `false`, and a fresh `RectTransform` defaults to `{x: 100, y: 100}`, not zero. Every row/group that didn't set an explicit `LayoutElement` AND didn't have a parent with `childControl*=true` was silently sitting at that raw 100×100 default. Fixed with an explicit `SetUpVerticalStack` helper (also catches the SAME framework default trap for `childForceExpand*`, which defaults `true` and was caught only after `Hand0Group` rendered 960 wide instead of a narrow column). Second lesson: **a batchmode editor script never renders a frame, so `sizeDelta` on a LayoutGroup-controlled RectTransform never gets recomputed at all** unless you force it — added `ForceLayoutRebuild` (`LayoutRebuilder.ForceRebuildLayoutImmediate`, 4 passes, since one pass doesn't converge a 4-level-deep hierarchy) called both at the end of each HUD build and explicitly bottom-up on `Hand0Group`/`Hand1Group` specifically, which is also a genuine player-facing win and not just a verification convenience — without it the first frame after Play would show everything at its wrong pre-rebuild size for one frame. Third, the one that actually explained the most confusing symptom (a fix that measured correct on `PlayerHandsRow` but never seemed to reach `Hand0Group` one level down): the mid-session switch from one centred `Panel` to `TableArea`/`ControlBar` left the OLD `Panel` hierarchy — including an old `Hand0Group` — orphaned in the scene, never touched by the new code, so a same-named search had no way to tell the live copy from the dead one. New `DestroyStaleChild` removes it going forward; the general lesson is that this project's "get-or-create, never delete" rule (true everywhere else in this tool) needs a deliberate, named exception whenever a GameObject gets RENAMED or RESTRUCTURED, not just added to.
- 2026-08-12 (Block 5 of `Docs/Prompts/2026-08-11-executive-day-plan.md`, code-only session - no clips recorded yet) - **`AudioManager` grows from a 35-line stub into a pooled sound player; every silent system that shipped this week gets a call site wired in.** Not more than that, per the plan's own "this is a party game, not an audio-driven one" - no mixer, no music, one ambience slot.
  New `Core/AudioCatalog` (sound id -> `AudioClip[]`, ScriptableObject, data only, same split as `VendorConfig`/`BallConfig`) and `Core/SoundId` (the nine ids every call site is allowed to use, one place so the catalog-seeding tools and the callers can't drift). `AudioManager` keeps its old `PlaySfx`-shaped API in spirit but the signature changes from `PlaySfx(AudioClip)` to `PlaySfx(string id)` - a real breaking change, but there were zero call sites to migrate (the stub was, in fact, never once called). Adds `PlaySfxAt` (positional, pooled - 12 round-robin one-shot `AudioSource`s as runtime-spawned children, "cut the oldest voice" rather than "find an idle one," because a pin rack breaking hits several ids in one frame and a silent 7th pin is worse than a truncated 1st), `PlaySfxAtSeeded` (variation picked from a caller-supplied seed instead of live `Random`), `GetClip` (for a caller that owns its own looping source), and `SetAmbience` (one venue-wide loop). Missing id, missing clip, or no `AudioManager.Instance` at all are ALL silent, never an exception - same contract the old stub already had, just honoured everywhere now.
  **The seeded-vs-plain split, applied for real rather than left as a comment:** `BowlingBall`'s own sounds (lane impact on release, the roll loop's variation pick, the gutter thunk) are keyed off `LaunchParameters.Seed` via `PlaySfxAtSeeded` - this IS the throw, same reasoning the class already applies to its cone-jitter/wobble-phase math one function up. `Pin`'s crash sfx, the slot reel flicker, and every UI click use plain `UnityEngine.Random` (or no randomness at all) - nothing downstream reads which take played, so a variation differing machine-to-machine is fine, the same rule Block 1 already wrote down for `SlotHud`'s reel flicker.
  **New: gutter detection, which did not exist anywhere in the codebase before this.** `BowlingBall` gained a `gutterHalfWidth` field (cosmetic only, default matching `LaneConfig.Width`'s own default half-width) and a one-shot latch in `FixedUpdate` that fires `ball_gutter` the moment the ball's lateral offset (read via the existing `LaneFrame.LateralOf`, the same math aim/hook already use) first exceeds it. Deliberately read-only - it cannot affect physics or scoring, only when a sound plays - so a wrong number is a wrong-sounding throw, never a wrong-scoring one.
  **Slot jackpot and reel-stop, closing a TODO `SlotHud` left for itself last session:** that file's own comment said "the audio-venue-wide half is future work (AudioManager has no clip catalog yet)" - it does now. `slot_jackpot` fires once on the false->true edge of `BonusPullsRemaining > 0` (not every frame the banner is up, and a mid-bonus retrigger does not re-fire it) via non-positional `PlaySfx`, deliberately not `PlaySfxAt`, so it is heard venue-wide exactly as the exec-day plan specified for FREE FRAME. `slot_reel_stop` fires positionally when a real pull result lands.
  **Scope extended slightly beyond the original Block 5 prompt, on purpose:** that prompt predates Block 8 and only named `BowlingBall`, `Pin`, `TicketLedger` and `PlayerInteractor` as call sites - it couldn't have named `SlotHud`/`BlackjackHud`, which didn't exist yet. Since Block 8 landed first, this session also added `ui_click` to every real button in both HUDs (Leave/Hit/Stand/Double/Split/Deal), because leaving a freshly-built, freshly-silent Canvas out of an audio pass whose whole point is "the game is silent" would have been a false economy of scope discipline, not a real one.
  `TicketLedger.OnTransaction` (its own doc comment already calls it "everyone's" listener - the HUD, an audio cue, later a `ClientRpc`) gets exactly one new subscriber, in `GameManager.Awake`, filtering to `Delta > 0` so a purchase (a negative delta) doesn't get called a "dispense." One choke point, same as the ledger's own Rule 1.
  **Wiring, not just code:** `AudioManager` is created via `AddComponent` (unchanged) but now the shared `AudioCatalog` asset is loaded-or-created and wired onto it by both `GreyboxSceneBuilder` and `ThunderLanesVenueStationSetupTool`, seeded with all nine `SoundId`s via `AudioCatalog.EnsureIds` - additive-only, so it never touches a row Tony has already filled a clip into, same create-once discipline as `EconomyConfig`. One catalog asset shared across every scene (same convention as `EconomyConfig`/`VendorConfig`), not one per scene.
  **What's still silent:** literally everything, until clips exist. No clip was sourced this session - that's the ~15-minute CC0 grab (freesound.org / Kenney) the plan asks Tony to do by hand, dropped into the nine rows `AudioCatalog.asset` now has waiting for them (`ball_roll`, `ball_lane_impact`, `ball_gutter`, `pin_crash`, `ticket_dispense`, `slot_reel_stop`, `slot_jackpot`, `ui_click`, `ambience_alley`). Verified by full EditMode-assembly batchmode compile (Assembly-CSharp + Assembly-CSharp-Editor, zero errors) rather than by ear, since there is nothing to hear yet - a real Play-mode audio check is still owed once clips land.
- 2026-08-12 (later still - real clips landed for the first time) - **Tony dropped 20 recorded/sourced clips into `AUDIO MOVE ME`; 7 are wired end-to-end (real sound in the game), the rest are blocked or await a design call, both recorded here rather than silently skipped.**
  **Format problem found empirically, not assumed (CLAUDE.md rule 3):** tested by actually importing samples in Unity batchmode before wiring anything. `.flac` imports cleanly via Unity's real `AudioImporter`. `.m4a` does NOT - Unity falls back to `DefaultImporter` (generic binary, unusable as an `AudioClip`) with no error logged, which is exactly the kind of silent-failure trap that would have shipped nine broken catalog rows if it had been assumed to "just work" instead of tested. Nine of Tony's twenty files are `.m4a` (including `ball_roll`, `ball_lane_impact`, `slot_jackpot`, `ticket_dispense` - several of the highest-value matches) and are BLOCKED until re-exported as WAV/OGG/FLAC; no conversion tool (ffmpeg, VLC) was available on this machine to do it here.
  **Seven clips wired, by matching filename to obvious context:** `ball_gutter` <- `GutterBallThudAudio.wav`, `ui_click` <- `GenButtonClickAudio.wav`, `slot_reel_stop` <- `SlotMachineStopAudio.wav`, `ambience_alley` <- `BowlingAlleyAmbienceAudio.wav`. Three more were specific enough to warrant NEW ids rather than folding into an existing generic one: `match_start` (new) <- `ButtonClickStartBowlingAudio.wav`, layered on TOP of the generic `ui_click` `PlayerInteractor` already plays before any `Interact()` call, not a replacement for it - `LaneKioskInteractable.Interact()` fires it once `StartMatch` succeeds. `card_deal` (new) <- `SingleCardDealAudio.wav`, fires once per card inside `BlackjackHud.RevealCards`' existing per-card stagger loop, so a fresh deal and a Hit both sound right without new logic. `slot_lever_pull` (new) <- `SlotLeverPullAudio.flac`, fires the instant `SlotHud.OnPullClicked` is pressed, before the flicker coroutine starts - a distinct "yanked the lever" beat ahead of `slot_reel_stop`'s "it landed" beat.
  **New standing import rule, `Editor/AudioImportPostprocessor.cs`**, same shape as `ArtTexturePostprocessor`: any file under `Assets/_Project/Audio/` with "ambience" or "music" in its name gets Streaming + Vorbis on first import automatically. Written because the drop included multiple 25-125MB ambience/music WAVs, and Unity's default Load Type decompresses a clip fully into memory on load - fine for a gutter thunk, a real problem for a two-minute room tone loop at that size. `BowlingAlleyAmbience.wav` (41MB) confirmed getting `loadType: Streaming`/`compressionFormat: Vorbis` in its `.meta` after import; the short one-shots confirmed NOT touched (stayed at Unity's own defaults).
  **`.flac` added to `.gitattributes`' LFS rules** (`.m4a` too, pre-emptively, for whenever the format problem above gets fixed) - it was missing even though `.wav`/`.mp3`/`.ogg` were already there.
  Wiring verified by running `ThunderLanesVenueStationSetupTool.RunAndSave` in batchmode against the live scene (creates+seeds `AudioCatalog.asset`, wires it onto the scene's `AudioManager`) followed by a one-off batch script (written, run, then deleted - not a standing tool, since future clips go into the Inspector by hand same as every other config asset) that assigned each of the 7 clips to its catalog row by id. Confirmed by reading the saved `.asset`/`.meta` YAML directly, not by trusting the tool's own log. Scene diff is exactly 3 lines (`AudioManager.catalog`/`poolSize` wired, `BowlingBall.gutterHalfWidth` default baked in from Block 5) - nothing else touched. Full Runtime+Editor batchmode compile, zero errors.
  **Left for Tony, recorded rather than guessed at:** (1) the nine blocked `.m4a` files, once re-exported; (2) `BeerBottleOpenAudio`/`BeerBottlePourAudio`/`GlugGlugGlugAhhDrinkingAudio.m4a` imply a drink-purchase sound sequence on `BarStation`, but that's a design call (one sound? all three in order? gated behind Block 7's still-unbuilt drink meter?) as much as a wiring one; (3) `BirthdayRoomAmbienceAudio.wav` names a room that appears NOWHERE in any doc or scene object - genuinely unknown scope, not guessed at; (4) `DJBoothMusicAudio.wav` has a real anchor (`DJ_Booth_and_Stage` art object in the scene, and `VenueEconomyIdeas.md`'s own "DJ booth - pay coins to blast a horn or change the venue track" S4 idea) but no station - it's an unbuilt feature, and even once built, one global `ambience_alley` slot can't also carry DJ music without a second ambience channel, which is a small but real design decision; (5) `WalkingWoodFloorAudio (1)/(2).wav` imply footsteps, which don't exist as a system anywhere in `FirstPersonController` - two variations plus only one floor material (wood) covered raises real speed-vs-distance triggering questions that are a system to build, not a clip to drop in; (6) `pin_crash` - specified back in the original Block 5 ask (needs 3 variations) - never arrived in this drop at all.
- 2026-08-12 (later still - Tony answered the four open questions from the prior entry, and each answer built more than a clip hookup) - **New PositionalAmbience component, a real DJ booth, birthday-room ambience, a basic footstep system, and a sequenced drink sound - four small systems built off one round of Tony's creative-director calls, none of them guessed at.**
  **`Core/PositionalAmbience`** - a small reusable component (loop=true/spatialBlend=1 AudioSource in Awake, same shape `BowlingBall`'s roll loop already uses) for "play this one clip at this one point in space, forever." Built because `AudioManager.SetAmbience` is deliberately ONE global slot (already claimed by `ambience_alley`), and the birthday room and DJ booth both needed their own independent, permanent loop rather than fighting over that slot or requiring a second bespoke global channel.
  **DJ booth, built as a real `VenueStation`** (`Slop/DJBoothStation.cs`), anchored on the existing `DJ_Booth_and_Stage` art object exactly like every other station - measured, not guessed, same as `Bar_and_Lounge`/`Blackjack_Pit_Table`. Music plays via `PositionalAmbience` and is NOT gated by interaction - a DJ booth plays whether or not anyone's standing there. **Deliberately not interactable yet**: `VenueEconomyIdeas.md`'s own idea for this booth is "pay tickets to blast a horn or change the venue track," a real ticket sink, but there is no horn clip and Tony gave no price - inventing either would be exactly the kind of silent guess this project's docs warn against. `CanInteract` returns false / `GetPrompt` returns empty, same "an unusable interactable is never selected, so a real-looking prompt would only get introduced by accident" rule `LaneKioskInteractable` already established. The paid mechanic is explicit future work, recorded here so nobody mistakes the gap for an oversight.
  **Birthday room ambience** - Tony confirmed a real party room exists (north side, across from casino/DJ), and it turned out to already BE in the scene as art (`Party_Room_and_Cap`/`Floor_PartyRoom`/`Doorway_PartyRoom_Entry`/`GlassWall_PartyRoom_Front` - found by grep before asking, not guessed at). A bare `Ambience_PartyRoom` marker (no VenueStation, nothing to interact with, it's a room not a shop) carries the loop.
  **Footsteps**, first system of its kind in the project - `Player/FirstPersonController.Move()` gained a distance accumulator (`RoamConfig.FootstepStrideDistance`/`MinFootstepSpeed`, new Tony-tunable fields) that fires `footstep_wood` off `CharacterController.velocity` (the ACHIEVED movement, same source `DriveWalkAnimation` already trusts, so walking into a wall correctly goes quiet). Distance-triggered rather than time-triggered on purpose: sprinting already covers a stride faster without a second speed-aware formula to keep in sync with `WalkSpeed`/`SprintMultiplier`. Naturally gated to Roaming-with-actual-movement because it lives inside `Move()`, which only runs when `LocomotionEnabled` is true - no new mode check needed. **Wood-only, on purpose, not by accident**: only wood-floor clips were provided (two variations, both in one `footstep_wood` catalog row - `AudioManager`'s existing per-call variation pick handles alternating them, no new code needed for that part), and there is no floor-material detection system in this project. Building one wasn't asked for and isn't worth guessing the shape of.
  **Drink sequence** - Tony's call was explicit: open, then pour, then glug, in order, not one combined sound. `BarStation.Interact` now checks `PurchaseResult.Success` (Vendor's own success flag - the same one `OnPurchased`/`OnRefused` already key off) and, only on a granted purchase, runs a coroutine sequencing `drink_open` -> `drink_pour` -> `drink_glug` with two Tony-tunable delays. All three ids are silent - the three source clips (`BeerBottleOpenAudio`/`BeerBottlePourAudio`/`GlugGlugGlugAhhDrinkingAudio`) are still `.m4a`, the format confirmed unimportable in the prior entry - but the logic is correct and ready the instant converted files land; nothing here needed to wait for that.
  Both `ThunderLanesVenueStationSetupTool` placements (DJ booth, birthday room) follow the exact existing idiom: `GameObject.Find` the art anchor, `TryGetWorldBounds` for a position, loud SKIPPED report (not a silent no-op) if the anchor is missing, idempotent get-or-create so a re-run never duplicates or repositions something Tony's already nudged. Verified with the same discipline as every session today: `RunAndSave` run in batchmode against the live scene (scene diff is exactly the two new GameObjects, nothing else touched), a one-off seed script (written, run, deleted) assigned the four new clips, the saved `AudioCatalog.asset`/`.meta` YAML read directly to confirm real GUIDs landed (not trusted from a log line), and a full Runtime+Editor batchmode compile came back at zero errors.
- 2026-08-12 (later still - Tony re-exported the blocked files, closing out the audio pass) - **Every previously-blocked .m4a from this session's earlier entries is now a working .wav, plus one new clip (SlotSpinningAudio) that got its own real wiring rather than being folded into an existing sound.** 8 catalog rows filled in one pass: `ball_roll`, `ball_lane_impact`, `ticket_dispense`, `slot_jackpot` (the four highest-value matches flagged as blocked earlier), `drink_open`/`drink_pour`/`drink_glug` (the sequence built two entries ago, silent until now — no code changed, only clips landed), and a genuinely new one.
  **New `slot_spinning`**: a dedicated looping `AudioSource` on `SlotHud` (same "not pooled, needs to loop for a bounded duration" reasoning as `BowlingBall`'s roll loop and the earlier `PositionalAmbience` component) starts the instant `PullRoutine`'s flicker begins and stops the instant the real result lands, right before `slot_reel_stop` fires — the whirr stops, then the clunk. Nothing else in `SlotHud` changed shape; this is a pure addition alongside the existing flicker/reel-stop/jackpot logic.
  **`AudioCatalog` now has real clips in every row except one**: `pin_crash` is the sole remaining gap, and it always has been — it was the original Block 5 ask (three variations) and never arrived in either audio drop. Everything else that was ever silent for a FORMAT reason (not a design reason) is now live: ball roll and lane impact on every throw, ticket dispense on every payout, the slot jackpot fanfare, and the three-beat drink sequence on every bar purchase.
  Verified the same way as every clip-wiring pass this session: `ThunderLanesVenueStationSetupTool.RunAndSave` in batchmode (only touches `AudioCatalog.asset`, not the scene, since no new station was needed — just a new row), a one-off seed script (written, run, deleted) assigned all 8 clips, the saved `.asset` YAML read directly to confirm 8 real GUIDs landed, full Runtime+Editor batchmode compile at 0 errors.
